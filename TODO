 - Implement `block` message
 - Implement `getdata` message
 - Implement `getblocks` message
 - Implement `getheaders` message (is this even used in the mainline client?)
 - Implement `headers` message